import * as Schema from "effect/Schema"

import * as Models from "../../models.ts"

export const BenefitFeatureFlagProperties = Schema.Struct({})
export type BenefitFeatureFlagProperties = typeof BenefitFeatureFlagProperties.Type
