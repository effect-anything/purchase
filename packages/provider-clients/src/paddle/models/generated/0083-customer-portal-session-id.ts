import * as Schema from "effect/Schema"

import * as Models from "../../models.ts"

export const CustomerPortalSessionId = Schema.String
export type CustomerPortalSessionId = typeof CustomerPortalSessionId.Type
