import * as Schema from "effect/Schema"
import * as Models from "../../models.ts"

export const PaymentMethodPaynow = Schema.Struct({

})
export type PaymentMethodPaynow = typeof PaymentMethodPaynow.Type
