import * as Schema from "effect/Schema"

import * as Models from "../../models.ts"

export const PaymentMethodMultibanco = Schema.Struct({})
export type PaymentMethodMultibanco = typeof PaymentMethodMultibanco.Type
