import * as Schema from "effect/Schema"
import * as Models from "../../models.ts"

export const PaymentMethodBillie = Schema.Struct({

})
export type PaymentMethodBillie = typeof PaymentMethodBillie.Type
