import * as Schema from "effect/Schema"
import * as Models from "../../models.ts"

export const PaymentMethodCustomerBalance = Schema.Struct({

})
export type PaymentMethodCustomerBalance = typeof PaymentMethodCustomerBalance.Type
