import * as Schema from "effect/Schema"

import * as Models from "../../models.ts"

export const PaymentMethodSwish = Schema.Struct({})
export type PaymentMethodSwish = typeof PaymentMethodSwish.Type
