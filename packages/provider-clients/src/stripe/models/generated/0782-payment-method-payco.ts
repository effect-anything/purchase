import * as Schema from "effect/Schema"

import * as Models from "../../models.ts"

export const PaymentMethodPayco = Schema.Struct({})
export type PaymentMethodPayco = typeof PaymentMethodPayco.Type
