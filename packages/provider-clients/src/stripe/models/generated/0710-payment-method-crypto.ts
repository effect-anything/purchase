import * as Schema from "effect/Schema"

import * as Models from "../../models.ts"

export const PaymentMethodCrypto = Schema.Struct({})
export type PaymentMethodCrypto = typeof PaymentMethodCrypto.Type
