import * as Schema from "effect/Schema"

import * as Models from "../../models.ts"

export const PaymentMethodPayByBank = Schema.Struct({})
export type PaymentMethodPayByBank = typeof PaymentMethodPayByBank.Type
