import * as Schema from "effect/Schema"

import * as Models from "../../models.ts"

export const PaymentMethodMobilepay = Schema.Struct({})
export type PaymentMethodMobilepay = typeof PaymentMethodMobilepay.Type
